{
  "name": "farrow-faas",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "example"
    ]
  },
  "devDependencies": {
    "@types/node": "^15.12.5",
    "lerna": "^4.0.0",
    "ts-node": "^10.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna clean --yes && rm -rf node_modules",
    "setup": "lerna bootstrap && lerna run build"
  }
}
